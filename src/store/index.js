import Vue from 'vue';
import Vuex from 'vuex';
import Url from './App/url';

Vue.use(Vuex);

export default new Vuex.Store({
  modules: {
    url: Url,
  },
  state: {
  },
  mutations: {
  },
  actions: {
  },
});
